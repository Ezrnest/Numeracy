# Created by lyc at 2020/11/23 13:29
import numpy as np
import matplotlib.pyplot as plt

from numeracy.interpolate.PolyInterpolate import *

# f = lambda x: 1. / (1 + 25 * x ** 2)
# n = 10
# xs = np.linspace(-1.0, 1.0, n + 1)
#
# Lag = interLagrange(f, xs)
# Lin = interLinearPiece(f, xs)
# S = interSpline3(f, xs)
#
# xs = np.linspace(-1.0,1.0,101)
#
# ys = [f(x) for x in xs]
# y1s = [Lag(x) for x in xs]
# y2s = [Lin(x) for x in xs]
# y3s = [S(x) for x in xs]
#
#
# plt.plot(xs, y1s)
# plt.plot(xs, ys)
# plt.show()
# plt.plot(xs, y2s)
# plt.plot(xs, ys)
# plt.show()
# plt.plot(xs, y3s)
# plt.plot(xs, ys)
# plt.show()
